import { SelectableByMouseDirective } from './selectable-by-mouse.directive';

describe('SelectableByMouseDirective', () => {
  it('should create an instance', () => {
    const directive =null;
    expect(directive).toBeTruthy();
  });
});
